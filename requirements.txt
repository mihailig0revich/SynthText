# Runtime dependencies for the SynthText generator.
# Pinned for Python 3.10 compatibility. Newer NumPy/SciPy/Matplotlib lines
# already require Python 3.11+, so do not blindly bump them on Python 3.10.

numpy==2.2.6
scipy==1.15.3
opencv-python==4.13.0.92
pillow==12.2.0
h5py==3.16.0
pygame==2.6.1

# Matplotlib is used by --viz and visualization scripts. Its transitive pins
# below keep Python 3.10 installs deterministic and avoid Python >=3.11 wheels.
matplotlib==3.10.9
contourpy==1.3.2
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.4.9
packaging==25.0
pyparsing==3.2.5
python-dateutil==2.9.0.post0
six==1.17.0
